# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=railway_db

# pgAdmin
PGADMIN_EMAIL=ayour_email@example.com
PGADMIN_PASSWORD=your_password
PGADMIN_PORT=5050

# PostgreSQL connection string for applications
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

